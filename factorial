#include<stdio.h>
int main()
{
	int i,n,fact=1;
	printf("Enter any number:");
	scanf("%d",&n);
	if(n<0)
	{
		printf("No factorial for negactive numbers");
	}
	else if(n==0)
	{
		printf("0!=1");
	}
	else
	{
	   for(i=1;i<=n;i++)
	   {
		   fact=fact*i;
	   }
	   printf("%d!=%d",n,fact);
    }  
}
